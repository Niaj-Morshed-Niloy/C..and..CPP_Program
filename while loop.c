#include<stdio.h>
main()
{
    int count=1,n;
    float x,y=1.0;
    scanf("%f%d",&x,&n);
    while(count<=n)
    {
        y=y*x;
    {
        count++;
    }
    printf("\n x=%f;n=%d; x to power n=%f \n",x,n,y);
}
getch();
}
